---
type: session-memory
date: 2026-07-04
feature: 013.005.008-T (ACP-native tool re-home)
epic: 013-F (ACP-Only Remote Controller)
status: in-progress
---

# Session Memory — F.6 shipment + 013.005.008-T phase 1 (T8.1)

## Completed this session

### Shipment 002-S (F.6 review follow-ups) — MERGED (PR #33 → `09feb50`)
- 013.006.001-T: serialized concurrent `.intercom` queue writes (per-file in-process
  lock registry in `src/persistence/intercom_queue_repo.rs`) + 64-thread test.
- 013.006.002-T: clarified `/arc queue transfer` partial-failure message; extracted
  pure `format_transfer_response` in `src/slack/commands.rs` + tests.
- 013.006.003-T: covered `wait_resume_instruct` thread-detection via pure
  `resolve_wait_instruct_route` in `src/slack/handlers/wait.rs` + tests.
- Closure done MANUALLY (see CLI bug below). Shipment/feature/tasks marked done.

### 013.005.008-T phase 1 — T8.1 MERGED (PR #34 → `ab87643`)
Standard ACP `session/request_permission` conformance (ADR-0016). Was silently
dropped → would deadlock a real agent. Now additive (bespoke `clearance` path
untouched).
Files: `src/acp/reader.rs` (parse + `json_rpc_id_key` helper), `src/driver/mod.rs`
(`AgentEvent::PermissionRequested` + `PermissionOption`), `src/driver/acp_driver.rs`
(`register_permission`, permission-aware `resolve_clearance`, `select_option_id`),
`src/main.rs` (`ClearanceRegistration` enum + event arm), tests in
`tests/unit/acp_permission_tests.rs` + `tests/contract/acp_driver_contract_tests.rs`.
Review round (3 valid P1, fixed in `5a05d08`): real ACP agents use NUMERIC
JSON-RPC ids; `AcpEnvelope.id` changed `Option<String>` → `Option<serde_json::Value>`;
exact id echoed in reply (type preserved); numeric-id tests added.

## CRITICAL: backlogit CLI bug (stashed high-pri)
`backlogit shipment ship` (CLI v1.3.0) is DESTRUCTIVE: it archived the parent epic
`013-F` (children still active → orphaned; `013.005-F` lost `parent_id`) and
re-stamped ALL prior archives (013.001-004) with the wrong merge SHA. REVERTED it
and did closure MANUALLY. Also `backlogit move`/`add` STRIP body sections (BEGIN/END
markers). **Until fixed: do closure via direct file edits** (set status done/shipped,
record SHA on shipment file), then `backlogit sync` + `doctor` to verify. backlogit
MCP server still DOWN (Transport closed) — use CLI `C:\Tools\backlogit.exe`.

## Merge protocol (in force)
Every PR: request Copilot review (via `gh api POST .../requested_reviewers -f
"reviewers[]=copilot-pull-request-reviewer[bot]"`) → wait → resolve ALL comments
(reply + `resolveReviewThread` graphql) → WAIT for explicit operator "PR N: merge
approved" → `gh pr merge N --merge --admin --delete-branch`. `--admin` only satisfies
the approver gate AFTER approval. Squash+rebase DISABLED; merge-commit only.

## Remaining 013.005.008-T subtasks (dependency-ordered)
- T8.2 (013.005.008.002-ST): create `src/acp/tools/mod.rs` + dispatcher in reader.rs;
  promote `src/slack/handlers/thread_reply.rs` text parsing to PRIMARY operator path
  (relay-as-text → parse → resolve_*). Deps: none. NEXT.
- T8.3 (…003-ST): approval+policy text-reply (ApprovalRepo, PolicyLoader/Evaluator).
  Deps: T8.1✓, T8.2.
- T8.4 (…004-ST): status path (broadcast `session/update` + heartbeat local ext).
  Deps: T8.2.
- T8.5 (…005-ST): operator-relay (transmit+standby) text-reply (PromptRepo,
  thread_reply, resolve_prompt/resolve_wait). Deps: T8.2.
- T8.6 (…006-ST): recover_state local ext (recovery repos). Deps: T8.2.
- T8.7 (…007-ST): CUTOVER — make `main.rs` not start HTTP transport for ACP tool
  access; verify no ACP path hits HTTP MCP endpoint. Unblocks 013.005.003-T
  (irreversible `src/mcp/*` deletion). Deps: T8.3-8.6.

## Operator decisions shaping the re-home (D1/D2/D3)
- D1: DROP `accept_diff` (Copilot mobile owns diffs).
- D2: NO Slack buttons — operator decisions are plain TEXT replies via thread_reply
  → AcpDriver::resolve_*.
- D3: DROP `set_operational_mode` (ACP is remote-by-design).

## Key architecture facts
- ACP event flow: `src/acp/reader.rs::parse_inbound_line` → `AgentEvent` →
  `src/main.rs` event consumer (line ~757 match) → `handle_*` → Slack + driver register.
- `AcpDriver` (`src/driver/acp_driver.rs`): per-session `stream_writers` mpsc;
  pending_clearances/prompts/permissions maps keyed by request_id string.
- `AgentDriver` trait (`src/driver/mod.rs`) shared by McpDriver + AcpDriver; permission
  is ACP-only, wired through the existing `resolve_clearance` (permission-aware).
- Plan doc: `docs/plans/2026-07-04-acp-native-tool-serving-plan.md`.
- Approved wire standard: ADR-0016 (`docs/adrs/0016-*`).

## Next steps
Ship T8.2 next on a fresh branch off main. Follow test-first + merge protocol.
Todos tracked in session SQLite (t8-1-permission done; t8-2..t8-7 pending).
