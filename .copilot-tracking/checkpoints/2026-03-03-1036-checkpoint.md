# Session Checkpoint

**Created**: 2026-03-03 10:36
**Branch**: 005-intercom-acp-server
**Working Directory**: D:\Source\GitHub\agent-intercom

## Task State

Phase 14 COMPLETE (commit ed2e6b9). Advancing to Phase 15.

Phase 15 tasks (from specs/005-intercom-acp-server/tasks.md):
- [ ] T135: Unit test — WebSocket drop notification (HITL-001)
- [ ] T136: Impl — reconnect hook in slack/client.rs
- [ ] T137: Impl — HTTP REST fallback post on drop/recover
- [ ] T138: Integration test — drop notification end-to-end
- [ ] T139: Unit test — audit event enum (HITL-007)
- [ ] T140: Impl — AuditEventType variants for ACP lifecycle
- [ ] T141: Impl — audit writes in ACP session handlers
- [ ] T142: Integration test — audit entries persisted
- [ ] T143: Unit test — TokenBucket rate limiter (ES-005)
- [ ] T144: Impl — TokenBucket struct in acp/reader.rs
- [ ] T145: Impl — config field rate_limit_msgs_per_sec
- [ ] T146: Integration test — rate exceeded terminates session
- [ ] T147: Unit test — stall timer init from DB (ES-006)
- [ ] T148: Impl — startup query for last_activity_at
- [ ] T149: Integration test — timer offset from DB timestamp
- [ ] T150: Unit test — session committed before reader start (ES-007)
- [ ] T151: Impl — reorder spawn sequence in commands.rs
- [ ] T152: Integration test — reader start guard
- [ ] T153: Unit test — read lock during channel resolution (ES-009)
- [ ] T154: Impl — hold read lock scope in session creation

## Session Summary

Phase 14 (Security Hardening) is complete. All 13 tasks (T122–T134) were
implemented and committed: ES-004 process tree termination via platform-specific
kill_process_tree/kill_process_group, ES-010 host_cli path validation with WARN
logging for suspicious paths, ES-008 outbound sequence numbers stamped on every
ACP message with write-failure marking session Interrupted. 831 tests pass, clippy
pedantic clean, formatted.

## Files Modified

| File | Change |
| ---- | ------ |
| src/acp/spawner.rs | Added process_group(0) (Unix) + CREATE_NEW_PROCESS_GROUP (Windows) to spawn_agent; added kill_process_tree, kill_process_group, check_for_orphan_processes |
| src/acp/writer.rs | Completely rewritten: generic W: AsyncWrite, counter: Arc<AtomicU64>, db: Arc<SqlitePool>, seq stamping, write-failure → Interrupted |
| src/config.rs | Added validate_host_cli_path method; is_host_cli_on_path and is_standard_host_cli_location private helpers |
| src/driver/acp_driver.rs | Added seq_counters: SeqCounterMap; register_session returns Arc<AtomicU64>; deregister cleanup |
| src/main.rs | ACP startup block calls validate_host_cli_path().ok() + agent_intercom::acp::spawner::check_for_orphan_processes |
| src/slack/commands.rs | Updated register_session call; updated run_writer with seq_counter + db; added tree-kill in handle_session_stop |
| tests/integration/acp_lifecycle_tests.rs | Updated register_session call; added T122 (Windows) and T123 (Unix) |
| tests/integration/acp_mcp_bridge_tests.rs | Fixed doc_markdown clippy violations |
| tests/unit/acp_codec_tests.rs | Added T130 (monotonic seq) and T131 (write failure) |
| tests/unit/command_tests.rs | Fixed doc_markdown clippy violations |
| tests/unit/config_tests.rs | Added T127 (validate_host_cli_path, 3 tests) |

## Files in Context

- specs/005-intercom-acp-server/tasks.md — Phase 14 tasks T122–T134 (done); Phase 15 tasks T135–T154 (next)
- specs/005-intercom-acp-server/spec.md — FR-042 (HITL-001), FR-043 (HITL-007), FR-044 (ES-005), FR-045 (ES-006), FR-046 (ES-007), FR-047 (ES-009)
- src/acp/spawner.rs — Modified: process group + tree-kill added
- src/acp/writer.rs — Modified: generic W, seq counter, db pool
- src/acp/reader.rs — Source for Phase 15 rate-limiting work (T143–T146)
- src/driver/acp_driver.rs — Modified: seq_counters field
- src/slack/client.rs — Source for Phase 15 WebSocket reconnect hook (T136)
- src/config.rs — Modified: validate_host_cli_path
- src/main.rs — Modified: ACP startup validation
- src/slack/commands.rs — Modified: updated writer/register calls; tree-kill on stop
- src/orchestrator/stall_detector.rs — Source for Phase 15 stall timer init from DB (T148)
- src/persistence/session_repo.rs — Source for ES-006 last_activity_at query (T148)
- src/audit/writer.rs — Source for Phase 15 audit logging (T139–T142)

## Key Decisions

1. **main.rs binary crate path**: References library as `agent_intercom::acp::spawner::...` not `crate::acp::spawner::...` — the binary is a separate crate.
2. **Clippy map_unwrap_or**: `option.map(f).unwrap_or_else(g)` must be `option.map_or_else(g, f)` for clippy pedantic compliance.
3. **case_sensitive_file_extension_comparisons**: Use `Path::extension().is_some_and(|ext| ext.eq_ignore_ascii_case("exe"))` rather than `str::ends_with(".exe")`.
4. **run_writer signature**: Changed from `(session_id, stdin: ChildStdin, msg_rx, cancel)` to generic `<W: AsyncWrite>(session_id, stdin: W, msg_rx, cancel, counter: Arc<AtomicU64>, db: Arc<SqlitePool>)`.
5. **register_session return**: Changed from `()` to `Arc<AtomicU64>` — all callers must capture return value.

## Failed Approaches

- Initial test run failed because `agent-intercom.exe` was locked by running processes; resolved by using `CARGO_TARGET_DIR=target\test-build`.

## Open Questions

- None blocking Phase 15.

## Next Steps

**Phase 15: Reliability & Observability**

Build feature 005-intercom-acp-server phase 15

Tasks:
1. T135: Write test for WebSocket drop notification in slack/client.rs
2. T136: Add reconnect hook calling a notify_fn on drop/recover
3. T137: Post HTTP REST Slack message (not Socket Mode) on WS drop/recover
4. T138: Integration test for drop notification E2E
5. T139: Unit test — AuditEventType enum for ACP lifecycle events
6. T140: Add ACP lifecycle variants to audit event system
7. T141: Wire audit writes into ACP session start/stop/pause/resume handlers
8. T142: Integration test — verify audit entries in DB
9. T143: Unit test — TokenBucket struct for rate limiting
10. T144: Implement TokenBucket in acp/reader.rs
11. T145: Add rate_limit_msgs_per_sec to GlobalConfig (default 10)
12. T146: Integration test — flood terminates session
13. T147: Unit test — stall timer offset from DB last_activity_at
14. T148: Implement startup query: compute elapsed = now - last_activity_at for each active session
15. T149: Integration test — timer fires sooner when DB shows recent activity
16. T150: Unit test — session persisted before reader spawned
17. T151: Reorder commands.rs spawn sequence: DB commit → driver register → reader spawn
18. T152: Integration test — no race on fast-starting agent
19. T153: Unit test — channel resolution holds read lock snapshot
20. T154: Wrap workspace_mappings.read() scope in commands.rs session creation

## Recovery Instructions

To continue this session's work, read this checkpoint file and:

- specs/005-intercom-acp-server/tasks.md (T135–T154 for Phase 15)
- specs/005-intercom-acp-server/spec.md (FR-042 through FR-047)
- src/slack/client.rs (WebSocket reconnect logic)
- src/acp/reader.rs (rate limiter insertion point)
- src/orchestrator/stall_detector.rs (timer init from DB)
- src/persistence/session_repo.rs (last_activity_at query)
- src/audit/writer.rs (audit event system)
- .copilot-tracking/memory/2026-03-03/005-intercom-acp-server-phase-14-memory.md
