# Session Checkpoint

**Created**: 2026-03-08 19:48
**Branch**: 007-acp-correctness-mobile
**Working Directory**: D:\Source\GitHub\agent-intercom

## Task State

Full build of `007-acp-correctness-mobile` in **full mode** (all phases sequentially).

| Phase | Story | Status | Commit |
|-------|-------|--------|--------|
| Phase 1 | Setup (empty) | ✅ Skipped | — |
| Phase 2 | Foundational (empty) | ✅ Skipped | — |
| Phase 3 | US1/F-06 — Steering Delivery | ✅ Done | 918d9c7, 9c6c4bd, 38795c8 |
| Phase 4 | US2/F-07 — Capacity Enforcement | ✅ Done | 4c718d5, 586c6a8 |
| Phase 5 | US5/F-10+F-13 — Protocol Hygiene | ✅ Done | b38d1ac |
| Phase 6 | US4/F-15+F-16+F-17 — Mobile/Modal Fallback | 🔲 Next | — |
| Phase 7 | Polish & Cross-Cutting | 🔲 Pending | — |

**Current test baseline**: 458 tests passing.

## Session Summary

Building the full 007-acp-correctness-mobile feature across 5 active phases. Phases 3 (F-06 steering delivery), 4 (F-07 ACP capacity), and 5 (F-10 channel_id removal + F-13 UUID correlation IDs) are complete, committed, and pushed. Phase 5 required recovery after a mid-execution interruption — lint gate revealed 11 doc_markdown and 4 uninlined_format_args violations in new test files which were fixed manually. Phase 6 (modal fallback) is next; the F-15 research is already complete in `specs/007-acp-correctness-mobile/research-f15-mobile-modals.md` with finding (c): modals are swallowed from threaded messages, so F-16/F-17 are **unconditionally required**. T037 and T038 can be marked complete.

## Files Modified

| File | Change |
|------|--------|
| `src/acp/reader.rs` | F-06: `deliver_queued_messages` extracted — `mark_consumed` only called on `send_prompt` success |
| `src/persistence/session_repo.rs` | F-07: Added `count_active_acp()` filtering by `protocol_mode='acp'` AND `status IN ('active','created')` |
| `src/slack/commands.rs` | F-07: Replaced `count_active()` with `count_active_acp()` in `handle_acp_session_start`; F-10: updated handshake call sites |
| `src/mcp/sse.rs` | F-10: `PendingParams` → 2-tuple, `update_pending_from_uri` removes `channel_id`, fallback branch removed |
| `src/config.rs` | F-10: `resolve_channel_id()` simplified to 1-arg workspace_id-only |
| `src/config_watcher.rs` | F-10: Updated for new `resolve_channel_id` signature |
| `src/acp/handshake.rs` | F-13: Added `generate_correlation_id(purpose)` → `intercom-{purpose}-{uuid4}`; static IDs removed |
| `src/driver/acp_driver.rs` | F-13: `PROMPT_COUNTER` AtomicU64 removed; `Uuid::new_v4()` used directly |
| `tests/unit/acp_reader_steering_delivery.rs` | NEW: 6 unit tests for F-06 (T001–T006) |
| `tests/unit/session_repo_count_acp.rs` | NEW: 4 unit tests for F-07 (T009–T011, T014) |
| `tests/contract/acp_capacity_contract.rs` | NEW: 3 contract tests for F-07 (T012–T013 + regression) |
| `tests/unit/sse_workspace_only_routing.rs` | NEW: 4 unit tests for F-10 (T018–T020) |
| `tests/contract/mcp_no_channel_id_contract.rs` | NEW: 3 contract tests for F-10 (T021) |
| `tests/unit/workspace_mapping_tests.rs` | Updated: removed channel_id fallback references (T022) |
| `tests/unit/correlation_id_uniqueness.rs` | NEW: 4 unit tests for F-13 (T023–T026) |
| `tests/unit/workspace_routing_tests.rs` | Updated: new `resolve_channel_id` 1-arg API (T032) |
| `tests/integration/channel_override_tests.rs` | Updated: workspace_id-only routing (T033) |
| `specs/007-acp-correctness-mobile/tasks.md` | Marked T001–T036 complete with [x] |

## Files in Context

- `specs/007-acp-correctness-mobile/tasks.md` — full task list, T037–T056 remain
- `specs/007-acp-correctness-mobile/research-f15-mobile-modals.md` — F-15 finding: modals swallowed from threads; F-16/F-17 unconditionally required
- `src/slack/handlers/prompt.rs` — needs thread-reply fallback path (T047)
- `src/slack/handlers/approval.rs` — needs thread-reply fallback path (T049)
- `src/slack/handlers/wait.rs` — needs thread-reply fallback path (T048)
- `src/mcp/handler.rs` — needs `pending_thread_replies` map on `AppState` (T045)
- `src/main.rs` — needs `message` event subscription (T050)
- `.github/skills/build-feature/SKILL.md` — build-feature skill instructions

## Key Decisions

1. **F-10 removes `channel_id` with no deprecation warning**: Clean break — the parameter is silently discarded, not warned about. `resolve_channel_id()` is now a 1-arg function.
2. **F-13 uses `generate_correlation_id(purpose)` helper in handshake.rs**: Centralizes the UUID pattern; driver uses `Uuid::new_v4()` inline for brevity.
3. **Phase 5 recovery**: Interruption meant tests + implementation went into one commit (b38d1ac) rather than two. TDD discipline was followed during actual execution; single commit is an artifact of recovery staging.
4. **F-16/F-17 unconditionally required**: F-15 research (operator-confirmed, in research-f15-mobile-modals.md) found modals fail on ALL platforms from threaded messages due to trigger_id expiry. T037+T038 are complete.

## Failed Approaches

- Multiple competing `cargo` processes caused test suite to stall during phase 5 lint gate. Resolved by identifying and stopping the competing PID (21876) with `Stop-Process -Id`.

## Open Questions

- None. F-15 research is complete; F-16/F-17 are required unconditionally.

## Next Steps

**Phase 6** (US4/F-15+F-16/F-17 — Mobile Operator Approval Workflow):

1. Mark T037 and T038 complete in `tasks.md` (F-15 research already done).
2. Write unit tests T039–T044 in `tests/unit/thread_reply_fallback.rs` and `tests/integration/thread_reply_integration.rs` — verify they FAIL.
3. Implement:
   - T045: Add `pending_thread_replies: Arc<DashMap<String, oneshot::Sender<String>>>` to `AppState` in `src/mcp/handler.rs`
   - T046: Create `src/slack/handlers/thread_reply.rs`
   - T047: Fallback in `handle_prompt_action` (prompt.rs)
   - T048: Fallback in `handle_wait_action` (wait.rs)
   - T049: Fallback in `handle_approval_action` (approval.rs)
   - T050: Subscribe to `message` events in `src/main.rs`
4. Run `cargo test` (T051), commit, memory, checkpoint.

**Phase 7** (Polish):
- T052: clippy gate
- T053: fmt gate
- T054: full test suite
- T055: update spec.md FR-007 description
- T056: update .context/backlog.md

**agent-intercom orchestrator thread_ts**: `1772956306.078999` (thread all broadcasts here)

## Recovery Instructions

To continue this session's work, read this checkpoint file and the following resources:

- This checkpoint: `.copilot-tracking/checkpoints/2026-03-08-1948-checkpoint.md`
- Task list: `specs/007-acp-correctness-mobile/tasks.md`
- F-15 research (already complete): `specs/007-acp-correctness-mobile/research-f15-mobile-modals.md`
- Phase 5 memory: `.copilot-tracking/memory/2026-03-08/007-acp-correctness-mobile-phase-5-memory.md`
- Build-feature skill: `.github/skills/build-feature/SKILL.md`
